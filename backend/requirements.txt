# backend/requirements.txt
# ATLAS AI 2.0 backend — pinned dependencies.
# Install:  pip install -r requirements.txt

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20          # form data + file uploads

# --- Database (async MySQL) ---
sqlalchemy[asyncio]==2.0.36
aiomysql==0.2.0                   # async MySQL driver (pure Python, no compiler)
pymysql==1.1.1                    # sync driver (Alembic migrations)
alembic==1.14.0
cryptography==44.0.0              # required for MySQL 8 caching_sha2 auth

# --- Cache / rate-limit (optional) ---
redis==5.2.1

# --- Config / validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0            # enables pydantic EmailStr

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1                     # pinned: passlib 1.7.4 breaks on bcrypt>=4.1

# --- Free AI providers (rotation in ai_provider_router.py) ---
groq==0.13.1
google-generativeai==0.8.3
httpx==0.28.1                     # used for Cerebras / SambaNova / Chatterbox

# --- PDF / documents (Resume AI, reports) ---
reportlab==4.2.5
pdfplumber==0.11.4
python-docx==1.1.2
pypdf==5.1.0                      # Career Target & Gap Engine resume parsing

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
aiosqlite==0.20.0                 # in-memory async DB for tests

# NOTE (added in a later batch, heavy download ~1GB):
#   sentence-transformers==3.3.1  # Jobs match score + resume-vs-JD embeddings